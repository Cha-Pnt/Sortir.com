
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">

        <title>Login | Sortir.com</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css2?family=Forum&family=Lobster+Two&family=Orbitron:wght@400;500&family=Public+Sans&family=ZCOOL+QingKe+HuangYou&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="css/login.css">
    <link rel="shortcut icon" href="{{ asset('images/cocktail-icone.png') }}"/>
</head>

<body>
    <div class="container min-vh-100" >
        <div class="row min-vw-100 rowLogin">
            <h1 class="titre">Sortir.com</h1>
                <div class="form">
                    <form method="post">
                        {% if error %}
                            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}

                        {% if app.user %}
                            <div class="mb-3">
                                You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                            </div>
                        {% endif %}


                        <div class="name">
                            <input type="text" value="{{ last_username }}" name="name" id="inputName" class="form-control" required autofocus placeholder="Identifiant"><br><br>
                        </div>

                        <div class="password">
                            <input type="password" name="password" id="inputPassword" class="form-control" required placeholder="Mot de passe">
                        </div>
                        <br><br>

                        <input type="hidden" name="_csrf_token"
                               value="{{ csrf_token('authenticate') }}">

                        <div class="group">
                            <div class="button">
                                <button class="btn btn-primary" type="submit">
                                   <strong>Se connecter</strong>
                                </button>
                            </div>
                            <div class="checkbox mb-3">
                                <label>
                                    <input class="remember" type="checkbox" name="_remember_me"> Se souvenir de moi
                                </label>
                            </div>
                        </div>

                    </form>
                </div>
        </div>
    </div>
</body>
</html>

